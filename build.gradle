allprojects {
    group = 'dev.gradleplugins'
}

// Configure javadoc
allprojects {
    pluginManager.withPlugin('java') {
        tasks.named('javadoc', Javadoc) {
            exclude('**/internal/**')
        }
    }
}
