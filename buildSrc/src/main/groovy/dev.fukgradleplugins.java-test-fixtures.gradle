plugins {
    id 'java-test-fixtures'
}

components.named("java", AdhocComponentWithVariants) {
    it.withVariantsFromConfiguration(configurations.testFixturesApiElements) { skip() }
    it.withVariantsFromConfiguration(configurations.testFixturesRuntimeElements) { skip() }
}
