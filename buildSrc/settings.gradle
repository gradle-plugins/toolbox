buildscript {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://repo.nokee.dev/release' }
    }
    dependencies {
        classpath 'dev.gradleplugins:gradle-plugin-development:1.3'
    }
}
apply plugin: 'dev.gradleplugins.gradle-plugin-development'