buildscript {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://repo.nokeedev.net/release' }
    }
    dependencies {
        classpath 'dev.gradleplugins:gradle-plugin-development:1.1.37'
    }
}
apply plugin: 'dev.gradleplugins.gradle-plugin-development'